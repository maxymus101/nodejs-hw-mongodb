description: Bad request due to validation error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Bad request
        data:
          type: object
          required:
            - path
            - message
          properties:
            path:
              type: string
              example: "field"
            message:
              type: string
              example: "Validation failed"
